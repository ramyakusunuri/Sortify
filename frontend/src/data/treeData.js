// ── TREE DATA: COMPLEXITIES ───────────────────────────────────
const TREE_COMPLEXITIES = {
  'Red-Black Tree': {
    rows: [
      ['Search',   'O(log n)', 'O(1)',    'Height balanced ≤ 2log₂(n+1)'],
      ['Insert',   'O(log n)', 'O(1)',    'At most 2 rotations'],
      ['Delete',   'O(log n)', 'O(1)',    'At most 3 rotations'],
      ['Rotation', 'O(1)',     'O(1)',    'Pointer updates only'],
    ],
    stable: false, inPlace: true,
    badges: ['Self-Balancing BST', 'Used in std::map (C++)']
  },
  'B-Tree': {
    rows: [
      ['Search', 'O(log n)', 'O(1)',    'Fan-out reduces tree height drastically'],
      ['Insert', 'O(log n)', 'O(1)',    'Key promotions and splits'],
      ['Delete', 'O(log n)', 'O(1)',    'Key rotations and merges'],
      ['Range',  'O(log n + k)', 'O(1)', 'k = number of results'],
    ],
    stable: false, inPlace: false,
    badges: ['Disk-Optimized', 'Used in Databases/Filesystems']
  },
  '2-3 Tree': {
    rows: [
      ['Search', 'O(log n)', 'O(1)',    '2-nodes and 3-nodes, always balanced'],
      ['Insert', 'O(log n)', 'O(log n)', 'May require cascading splits'],
      ['Delete', 'O(log n)', 'O(log n)', 'May require cascading merges'],
      ['Height', 'O(log n)', 'O(1)',    'Height between log₃(n) and log₂(n)'],
    ],
    stable: false, inPlace: false,
    badges: ['Perfectly Balanced', 'Precursor to B-Trees']
  },
  'KD-Tree': {
    rows: [
      ['Build',           'O(n log n)', 'O(n)',    'Median split at each level'],
      ['Search (avg)',    'O(log n)',   'O(log n)', 'Best case, balanced tree'],
      ['Search (worst)',  'O(n)',       'O(log n)', 'Degenerate case, all branches'],
      ['Insert',          'O(log n)',   'O(1)',     'Like a BST insertion'],
    ],
    stable: false, inPlace: false,
    badges: ['Multi-dimensional', 'Used in ML/Nearest Neighbor']
  },
};

// ── TREE DATA: EXPLANATIONS ───────────────────────────────────
const TREE_EXPLANATIONS = {
  'Red-Black Tree': {
    how: 'A Red-Black Tree is a self-balancing Binary Search Tree where each node is colored either RED or BLACK. Five invariants are maintained: (1) Every node is red or black, (2) Root is always black, (3) Leaves (NIL) are black, (4) Red nodes have only black children, (5) Every path from root to leaf has the same number of black nodes. These rules ensure the tree height stays at most 2·log₂(n+1), guaranteeing O(log n) operations.',
    uses: ['C++ std::map and std::set implementation', 'Java TreeMap and TreeSet', 'Linux Completely Fair Scheduler (CFS)', 'Process/memory management in OS kernels'],
    props: ['Self-balancing via recoloring + rotations', 'At most 2 rotations per insertion', 'At most 3 rotations per deletion', 'Height ≤ 2·log₂(n+1) always', 'Black-height property ensures balance'],
  },
  'B-Tree': {
    how: 'A B-Tree of order t is a self-balancing search tree where each node can hold between t-1 and 2t-1 keys and have between t and 2t children. All leaves are at the same depth (perfectly balanced). When a node is full (2t-1 keys), it splits: the median key is promoted to the parent, and the node becomes two nodes. B-Trees are optimized for disk I/O because a single node read can bring many keys into memory at once.',
    uses: ['Database index structures (MySQL, PostgreSQL)', 'Filesystem metadata (NTFS, ext4, HFS+)', 'When data is too large to fit in RAM', 'Multi-level storage hierarchies'],
    props: ['All leaves at the same depth', 'Node holds t-1 to 2t-1 keys', 'Minimizes disk reads per operation', 'Fan-out drastically reduces tree height', 'Generalizes BST and 2-3 trees'],
  },
  '2-3 Tree': {
    how: 'A 2-3 Tree is a perfectly balanced search tree where internal nodes have exactly 2 or 3 children. A 2-node holds 1 key and has 2 children; a 3-node holds 2 keys and has 3 children. Insertions always happen at leaves. When a leaf becomes a 4-node (overflow), it splits and the middle key is promoted upward. This promotion can cascade all the way to the root, which may also split and increase the tree height.',
    uses: ['Foundation for understanding B-Trees', 'Teaching balanced tree concepts', 'Basis of Left-Leaning Red-Black Trees', 'When exact balance is required'],
    props: ['All leaves at identical depth (perfect balance)', 'Only 2-nodes and 3-nodes allowed', 'Growth happens at the root (splits up)', 'B-Tree of minimum degree 2', 'Easier to reason about than Red-Black Trees'],
  },
  'KD-Tree': {
    how: 'A KD-Tree (K-Dimensional Tree) is a binary tree that partitions k-dimensional space by cycling through axes. At depth 0, points are split by the x-axis median. At depth 1, by the y-axis median. And so on. This creates hyperplane partitions that allow efficient nearest-neighbor queries, range searches, and spatial lookups. The tree is built by recursively selecting the median along the current axis.',
    uses: ['Nearest neighbor search in Machine Learning', 'Collision detection in game engines', 'Range queries in spatial databases', 'Point cloud processing in robotics', 'Ray-tracing acceleration structures'],
    props: ['Partitions k-dimensional space recursively', 'Alternates split axis by depth', 'Median split ensures balance', 'Query pruning eliminates irrelevant subtrees', 'Degrades with high dimensionality (curse of dimensionality)'],
  },
};

// ── TREE DATA: PSEUDOCODE ─────────────────────────────────────
const TREE_PSEUDOCODES = {
  'Red-Black Tree': [
    'RB-INSERT(T, z):',
    '  BST-INSERT(T, z)',
    '  z.color = RED',
    '  while z.parent.color == RED:',
    '    if z.parent == z.parent.parent.left:',
    '      y = z.parent.parent.right  // uncle',
    '      if y.color == RED:         // Case 1',
    '        z.parent.color = BLACK',
    '        y.color = BLACK',
    '        z.parent.parent.color = RED',
    '        z = z.parent.parent',
    '      else:',
    '        if z == z.parent.right:  // Case 2',
    '          z = z.parent',
    '          LEFT-ROTATE(T, z)',
    '        z.parent.color = BLACK   // Case 3',
    '        z.parent.parent.color = RED',
    '        RIGHT-ROTATE(T, z.parent.parent)',
    '    else: (symmetric cases)',
    '  T.root.color = BLACK',
  ],
  'B-Tree': [
    'B-TREE-INSERT(T, k):',
    '  r = T.root',
    '  if r.n == 2t - 1:  // root is full',
    '    s = new node',
    '    T.root = s',
    '    s.children[0] = r',
    '    B-TREE-SPLIT-CHILD(s, 0)',
    '    B-TREE-INSERT-NONFULL(s, k)',
    '  else:',
    '    B-TREE-INSERT-NONFULL(r, k)',
    '',
    'B-TREE-INSERT-NONFULL(x, k):',
    '  if x.leaf:',
    '    insert k in sorted position',
    '  else:',
    '    find child c to descend',
    '    if c.n == 2t - 1:',
    '      B-TREE-SPLIT-CHILD(x, i)',
    '      if k > x.keys[i]: i++',
    '    B-TREE-INSERT-NONFULL(x.children[i], k)',
  ],
  '2-3 Tree': [
    'INSERT(T, v):',
    '  leaf = FIND-LEAF(T.root, v)',
    '  INSERT-IN-LEAF(leaf, v)',
    '  if leaf is 4-node:',
    '    SPLIT(leaf)',
    '',
    'SPLIT(node):',
    '  mid = node.keys[1]',
    '  left  = {keys[0]}',
    '  right = {keys[2]}',
    '  PROMOTE(mid, node.parent)',
    '  if parent is 4-node:',
    '    SPLIT(parent)  // cascade',
    '  if node was root:',
    '    new root = {mid}',
    '    root.children = [left, right]',
  ],
  'KD-Tree': [
    'KD-BUILD(points, depth):',
    '  if points is empty: return null',
    '  axis = depth mod k',
    '  sort points by axis',
    '  median = points[len/2]',
    '  node.point = median',
    '  node.left  = KD-BUILD(left half,  depth+1)',
    '  node.right = KD-BUILD(right half, depth+1)',
    '  return node',
    '',
    'KD-NEAREST(node, target, depth):',
    '  if node is null: return',
    '  axis = depth mod k',
    '  update best if node is closer',
    '  near  = closer half along axis',
    '  far   = other half',
    '  search near side first',
    '  if |target[axis] - node[axis]| < best:',
    '    search far side also (pruning)',
  ],
};

// ── TREE QUIZ DATA ────────────────────────────────────────────
const TREE_QUIZ_DATA = {
  'Red-Black Tree': [
    { q:'What color is the root of a Red-Black Tree always?', opts:['Red','Black','Either','Depends on height'], ans:1, exp:'The root must always be BLACK. This is one of the five Red-Black Tree invariants.' },
    { q:'What is the maximum height of a Red-Black Tree with n nodes?', opts:['n','log₂n','2·log₂(n+1)','n/2'], ans:2, exp:'The height is at most 2·log₂(n+1) because no path can be more than twice as long as any other path.' },
    { q:'In a Red-Black Tree, how many rotations are needed for insertion?', opts:['At most 1','At most 2','At most 3','Unlimited'], ans:1, exp:'Insertion requires at most 2 rotations (one left + one right in the worst case).' },
    { q:'What does Case 1 of RB-Tree insertion fixup involve?', opts:['Rotating left','Rotating right','Recoloring only','Splitting the node'], ans:2, exp:'Case 1 occurs when the uncle is RED — we just recolor the parent, uncle, and grandparent. No rotation needed.' },
    { q:'Which real-world system uses Red-Black Trees?', opts:['FAT filesystem','Python dict','Linux CFS scheduler','TCP/IP stack'], ans:2, exp:'The Linux Completely Fair Scheduler uses a Red-Black Tree to track runnable processes by virtual runtime.' },
    { q:'What color are NULL (leaf) nodes in a Red-Black Tree?', opts:['Red','Black','Uncolored','White'], ans:1, exp:'NULL sentinel nodes (NIL leaves) are always considered BLACK. This simplifies invariant checking.' },
    { q:'What property distinguishes a Red-Black Tree from an AVL Tree?', opts:['RB allows red nodes in sequence','RB is more strictly balanced','AVL allows red-black coloring','AVL has higher worst-case height'], ans:0, exp:'RB Trees are less strictly balanced than AVL Trees — they tolerate a factor-of-2 height difference using the red-black coloring scheme.' },
    { q:'After inserting a red node whose parent is also red, what must be done?', opts:['Nothing — two reds is fine','Delete and reinsert','Run the fixup (recolor/rotate)','Set the inserted node to black'], ans:2, exp:'Two consecutive RED nodes violate the RB Tree invariant. The fixup procedure (recoloring + rotations) must restore it.' },
    { q:'How many children does each internal node in a Red-Black Tree have?', opts:['1 or 2','Exactly 2','2 or 3','1 to 4'], ans:1, exp:'Red-Black Trees are binary trees — every internal node has exactly 2 children (possibly NIL sentinels).' },
    { q:'Black-height of a Red-Black Tree is defined as?', opts:['Total number of black nodes','Black nodes from root to any leaf','Height divided by 2','Number of rotations done'], ans:1, exp:'Black-height is the number of BLACK nodes on any path from the root to a leaf (not counting the root itself). It must be equal on all paths.' },
    { q:'What is the time complexity of searching in a Red-Black Tree?', opts:['O(1)','O(n)','O(log n)','O(n log n)'], ans:2, exp:'Because the height is O(log n), searching takes O(log n) time — same as balanced BST.' },
    { q:'In Case 3 of RB-Tree insertion, what operation is performed?', opts:['Recolor only','Single rotation + recolor','Double rotation','Split the tree'], ans:1, exp:'Case 3 requires a single rotation at the grandparent and recoloring the parent and grandparent.' },
    { q:'C++ std::map is typically implemented using?', opts:['Hash table','AVL Tree','Red-Black Tree','Skip List'], ans:2, exp:'Most implementations of std::map use a Red-Black Tree to guarantee O(log n) insertion, deletion, and search.' },
    { q:'Compared to AVL Trees, Red-Black Trees are faster for?', opts:['Lookups','Insertions and deletions','Range queries','Memory usage'], ans:1, exp:'RB Trees require fewer rotations on average for insertions and deletions, making writes faster than AVL Trees.' },
    { q:'What is the minimum number of nodes in a Red-Black Tree of black-height k?', opts:['k','2^k - 1','2^(k-1)','k²'], ans:1, exp:'The minimum occurs when all non-leaf nodes are black: 2^k - 1 black nodes with NIL leaves.' },
  ],
  'B-Tree': [
    { q:'What is the minimum number of keys in a B-Tree node of order t (except root)?', opts:['1','t-1','t','2t-1'], ans:1, exp:'Every non-root node must have at least t-1 keys. This ensures the tree stays balanced when merging.' },
    { q:'What is the maximum number of keys a B-Tree node of order t can hold?', opts:['t','t+1','2t','2t-1'], ans:3, exp:'A node can hold at most 2t-1 keys. When a node reaches 2t keys it is split.' },
    { q:'In a B-Tree, all leaves are at the same depth. This property ensures?', opts:['O(1) search','Perfectly balanced height','No need for rotations','All values are sorted'], ans:1, exp:'Perfect depth balance means every search path has the same length, guaranteeing O(log n) operations.' },
    { q:'When a B-Tree node is full during insertion, what happens?', opts:['The key is discarded','The node is split and median promoted','The root is replaced','The tree rebuilds from scratch'], ans:1, exp:'A full node is split into two nodes, and the median key is promoted to the parent node.' },
    { q:'Why are B-Trees preferred for database indexing over binary trees?', opts:['They use less memory','Higher fan-out reduces I/O operations','They are simpler to implement','They sort faster'], ans:1, exp:'B-Trees have high fan-out (many keys per node), so the tree is very short. This drastically reduces the number of disk reads needed.' },
    { q:'What real-world system heavily uses B-Trees?', opts:['TCP/IP routing','Python list','MySQL InnoDB storage engine','CPU cache'], ans:2, exp:'MySQL InnoDB uses B+ Trees (a variant of B-Trees) for its primary and secondary indexes.' },
    { q:'A B-Tree of order t=2 is also known as?', opts:['Red-Black Tree','AVL Tree','2-3-4 Tree','Splay Tree'], ans:2, exp:'A B-Tree with minimum degree t=2 allows 1 to 3 keys per node and 2 to 4 children, making it a 2-3-4 tree.' },
    { q:'The height of a B-Tree with n keys and minimum degree t is at most?', opts:['log₂ n','logₜ n','logₜ((n+1)/2)','n/t'], ans:2, exp:'Height ≤ logₜ((n+1)/2). Because of high fan-out, B-Trees are extremely flat — a trillion-key tree might have height 5.' },
    { q:'During B-Tree deletion, when is a key borrowed from a sibling?', opts:['Always','When the node has exactly t-1 keys','When the sibling has t or more keys','When the parent is full'], ans:2, exp:'If the sibling has at least t keys (more than minimum), we can rotate a key through the parent to fill the deficit.' },
    { q:'A B+ Tree differs from a B-Tree in that?', opts:['Leaves link to each other','Nodes have fewer keys','No rotations needed','Only integers are stored'], ans:0, exp:'In a B+ Tree, all data is stored at the leaves, which are linked in a list. This allows efficient range scans.' },
  ],
  '2-3 Tree': [
    { q:'In a 2-3 Tree, what is a 3-node?', opts:['A node with 3 children and 3 keys','A node with 2 keys and 3 children','A node with 1 key and 2 children','A leaf node with value 3'], ans:1, exp:'A 3-node has exactly 2 keys and 3 children. The three subtrees contain values respectively less than key1, between key1 and key2, and greater than key2.' },
    { q:'Where do insertions always begin in a 2-3 Tree?', opts:['At the root','At any internal node','At a leaf node','At the median node'], ans:2, exp:'Insertions always start at the appropriate leaf node. The leaf is found by searching down the tree, then the new key is added there.' },
    { q:'What happens when a 2-3 Tree leaf gets a third key (overflow)?', opts:['The key is moved to root','The node splits and median key is promoted','The tree is rebuilt','The key is dropped'], ans:1, exp:'When a node has 3 keys (overflow), it splits into two nodes. The middle key is promoted to the parent.' },
    { q:'How does the height of a 2-3 Tree grow?', opts:['From the bottom','From the root','From any node','Randomly'], ans:1, exp:'Height increases only when the root splits. A promoted key creates a new root, so the tree grows upward — all leaves remain at the same depth.' },
    { q:'A 2-3 Tree is a special case of which more general structure?', opts:['Red-Black Tree','AVL Tree','B-Tree','Heap'], ans:2, exp:'A 2-3 Tree is a B-Tree with minimum degree t=2 — nodes have 1 or 2 keys, and 2 or 3 children.' },
    { q:'What is the relationship between 2-3 Trees and Red-Black Trees?', opts:['They are identical','2-3 Trees are the conceptual model for Left-Leaning Red-Black Trees','Red-Black is faster for all operations','No relationship'], ans:1, exp:'Robert Sedgewick showed that LLRB Trees (a variant of Red-Black Trees) are equivalent to 2-3 Trees. Every 2-3 tree maps to exactly one LLRB tree.' },
    { q:'In a 2-3 Tree, all leaves are always?', opts:['Red colored','At the same depth','Holding 2 keys','Linked together'], ans:1, exp:'All leaves in a 2-3 Tree are at the same depth, ensuring perfect balance. This is the tree\'s key property.' },
    { q:'What is the maximum keys in a 2-3 Tree of height h?', opts:['2^h - 1','3^(h+1) - 1','h²','2h'], ans:1, exp:'A full 2-3 Tree of height h (root at h=0) has 3^(h+1)-1 keys maximum (all 3-nodes).' },
    { q:'If a split cascades all the way to the root in a 2-3 Tree, what happens?', opts:['The tree becomes unbalanced','A new root is created, height increases by 1','The insert fails','The deepest node is removed'], ans:1, exp:'If the root also overflows during a cascade, a new root is created with the promoted key, and the tree height increases by exactly 1.' },
    { q:'Compared to a BST, a 2-3 Tree has better worst-case performance because?', opts:['It uses hashing','Its height is always O(log n)','It avoids comparisons','It stores more data per node'], ans:1, exp:'A 2-3 Tree is always perfectly balanced by construction. Its height is always between log₃(n) and log₂(n), ensuring O(log n) for all operations.' },
  ],
  'KD-Tree': [
    { q:'What is the split axis at depth d in a 2D KD-Tree?', opts:['Always X-axis','Always Y-axis','d mod 2 (alternates X, Y)','Chosen randomly'], ans:2, exp:'In a KD-Tree, the split axis cycles through dimensions. At depth d, the axis is d mod k, so for 2D: depth 0 splits by X, depth 1 by Y, etc.' },
    { q:'What time complexity does building a KD-Tree from n points take?', opts:['O(n)','O(n log n)','O(n²)','O(log n)'], ans:1, exp:'Building a KD-Tree requires sorting at each level: O(n log n) total using median-of-medians or sort-then-split.' },
    { q:'KD-Trees are mainly used for?', opts:['Sorting integers','Nearest neighbor and range searches in k-dimensional space','Balancing a BST','Hash table collision resolution'], ans:1, exp:'KD-Trees excel at spatial queries — finding the closest point, all points within a radius, or points in a bounding box.' },
    { q:'In nearest-neighbor search, when can the KD-Tree prune (skip) a subtree?', opts:['Always','When the subtree has no leaves','When the best distance so far is less than the distance to the splitting hyperplane','When the target is the root'], ans:2, exp:'If the distance from the query point to the splitting hyperplane is greater than the current best distance, the entire far subtree can be skipped.' },
    { q:'What does KD stand for in KD-Tree?', opts:['Kernel Data','K-Dimensional','Key-Depth','Known Data'], ans:1, exp:'KD stands for K-Dimensional. The tree efficiently partitions k-dimensional space using hyperplane splits.' },
    { q:'A KD-Tree with all points on the same line (degenerate case) has search complexity?', opts:['O(1)','O(log n)','O(n)','O(n²)'], ans:2, exp:'In the worst case (highly degenerate or high-dimensional data), nearest neighbor search degrades to O(n) as pruning becomes ineffective.' },
    { q:'Which field heavily uses KD-Trees?', opts:['Cryptography','Computer Graphics and Machine Learning (k-NN)','Network routing','Database transactions'], ans:1, exp:'KD-Trees are widely used in k-Nearest Neighbor (k-NN) classification in ML and ray-tracing/collision detection in graphics.' },
    { q:'In a balanced 2D KD-Tree of n points, what is the average search complexity?', opts:['O(1)','O(log n)','O(√n)','O(n)'], ans:1, exp:'In a balanced KD-Tree with a small number of dimensions, nearest neighbor search takes O(log n) on average.' },
    { q:'What point is chosen as the split node at each level during KD-Tree construction?', opts:['The smallest point','The median point along the current axis','The average point','A random point'], ans:1, exp:'The median along the current axis is chosen so the tree stays balanced — roughly half the points go to each subtree.' },
    { q:'The "curse of dimensionality" affects KD-Trees by?', opts:['Increasing insert time','Making the tree wider','Reducing pruning effectiveness as k grows large','Causing key collisions'], ans:2, exp:'In very high dimensions (k >> 20), almost all points are roughly equidistant from any query point. Pruning fails and search degrades to O(n).' },
  ],
};
