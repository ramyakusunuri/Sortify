// ── PSEUDOCODE DATA ──────────────────────────────────────────
const PSEUDOCODES = {
  'Bubble Sort': [
    'procedure bubbleSort(arr):',
    '  n = length(arr)',
    '  for i = 0 to n-2:',
    '    for j = 0 to n-i-2:',
    '      if arr[j] > arr[j+1]:',
    '        swap(arr[j], arr[j+1])',
    '    mark arr[n-1-i] as sorted',
    '  mark arr[0] as sorted',
  ],
  'Selection Sort': [
    'procedure selectionSort(arr):',
    '  n = length(arr)',
    '  for i = 0 to n-2:',
    '    minIdx = i',
    '    for j = i+1 to n-1:',
    '      if arr[j] < arr[minIdx]:',
    '        minIdx = j',
    '    swap(arr[i], arr[minIdx])',
    '    mark arr[i] as sorted',
  ],
  'Insertion Sort': [
    'for i = 1 to n-1:',
    '  // arr[0..i-1] is sorted',
    '  key = arr[i]',
    '  j = i - 1',
    '  while j >= 0 and arr[j] > key:',
    '    arr[j+1] = arr[j]  // shift right',
    '    j = j - 1',
    '  arr[j+1] = key       // insert',
  ],
  'Merge Sort': [
    'procedure mergeSort(arr, l, r):',
    '  if l >= r: return',
    '  m = (l + r) / 2',
    '  mergeSort(arr, l, m)',
    '  mergeSort(arr, m+1, r)',
    '  merge(arr, l, m, r)',
    'procedure merge(arr, l, m, r):',
    '  compare L[i] with R[j]',
    '  place smaller into arr[k]',
    '  copy remaining elements',
    '  all merged elements are sorted',
  ],
  'Quick Sort': [
    'procedure quickSort(arr, lo, hi):',
    '  if lo >= hi: return (sorted)',
    '  pivot = arr[hi]',
    '  i = lo - 1',
    '  // partition loop',
    '  for j = lo to hi-1:',
    '    if arr[j] <= pivot:',
    '      i++, swap(arr[i], arr[j])',
    '    end if',
    '  swap(arr[i+1], arr[hi])  // place pivot',
    '  pivot is in final position',
    '  quickSort(arr, lo, p-1)',
    '  quickSort(arr, p+1, hi)',
  ],
  'Heap Sort': [
    'procedure heapSort(arr):',
    '  // Phase 1: Build Max-Heap',
    '  for i = n/2-1 to 0: heapify(arr, n, i)',
    '  // Phase 2: Extract elements',
    '  for i = n-1 to 1:',
    '    swap(arr[0], arr[i])',
    '    heapify(arr, i, 0)',
    'procedure heapify(arr, n, i):',
    '  largest = i, l = 2i+1, r = 2i+2',
    '  if arr[l] > arr[largest]: largest = l',
    '  if arr[r] > arr[largest]: largest = r',
    '  if largest ≠ i:',
    '    swap(arr[i], arr[largest])',
    '    heapify(arr, n, largest)',
  ],
  'Shell Sort': [
    'procedure shellSort(arr):',
    '  gap = n / 2',
    '  while gap > 0:',
    '    for i = gap to n-1:',
    '      temp = arr[i]',
    '      j = i',
    '      while j >= gap and arr[j-gap] > temp:',
    '        arr[j] = arr[j-gap]',
    '        j -= gap',
    '      arr[j] = temp',
    '    gap = gap / 2',
  ],
};
